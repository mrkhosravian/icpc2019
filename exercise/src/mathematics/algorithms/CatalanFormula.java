package mathematics.algorithms;

import annotations.Formula;
import annotations.Persian;
import jdk.jfr.Description;

@Persian("اعداد کاتالان")
@Formula("C(n) = 2n C n / (n+1)")
@Description("The number of distinct binary trees can be generalized as Catalan formula, denoted as C(n)")
public class CatalanFormula {
}
